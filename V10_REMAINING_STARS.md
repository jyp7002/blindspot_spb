# V10_REMAINING_STARS.md — numbers still awaiting a source

**GENERATED BY `src/v10_remaining.py` from `binary_debiaser_draft_v3.1.md`. Do not hand-edit.**

The v10 audit resolved **75 of 75** `*`-marked values. These **0** remain.

Every one of these was traced to the artifact the author named, and the named source was then checked. These are the ones where the check FAILED — the artifact exists, but it does not produce the printed value. That is a stronger statement than "unregistered": each needs a decision from the author, not more plumbing.

| status | meaning | count |
|---|---|---:|

---

---

## The two named blockers

These are registered in the manifest by id, so the audit fails on them by name rather than reporting an anonymous stray number:


## What each needs

Only the alpha-extension needs compute. Two of the others (`contrast_gap`, the DPO correlation) need a **population stated** — both are population-dependent estimates printed as single numbers, and in both cases the value that reproduces is not the value printed. The lineage interaction needs re-deriving or citing to the companion.

## Where the rest went

Of the 75 markers in `binary_debiaser_draft_v3.md`, 79 replacements across 61 resolved values were applied by `python3 src/v10_audit.py --replace`. The substantive corrections are tabled in `V10_FINDINGS.md`; the full per-id record is `results/v10/audit_report.json`.
